<h1>User::Users#favorites</h1>
<% content_for :mypage do %>
  <nav>
	<ul class="nav navbar-nav navbar-right">
	<li>
	  <%= link_to '新規投稿する', new_user_post_path, class: "btn-primary", style: "margin-left: 20px; margin-top: 33px;" %>
	</li>
	<li>
	  <%= link_to '自分のいいね', user_favorites_path, class: "btn-info", style: "margin-left: 5px; margin-top: 33px;" %>
	</li>
	<li>
	  <%= link_to 'にゃんこメモ', user_user_reminders_path(current_user.id), class: "btn-warning", style: "margin-left: 5px; margin-top: 33px;" %>
	</li>
	<li>
	<%= link_to "フォロー", root_path, class: "btn-success", style: "margin-left: 5px; margin-top: 33px;" %>
	</li>
	<li>
	  <%= link_to 'フォロワー', root_path, method: :delete, class: "btn-danger", style: "margin-left: 5px; margin-top: 33px;" %>
	</li>
	<br>
	  <p class="navbar-text navbar-right">
	    <%= current_user.name %>さんのマイページです。
	  </p>
	</ul>
	</nav>
<% end %>

<% if @favorites.present? %>
	<% @favorites.each do |favorite| %>
	<% post = Post.find_by(id: favorite.post_id) %>
	  <tr>
	  	<td><%= attachment_image_tag post.user, :image, :fill, 50, 50, format:'jpeg', fallback: "no_image.jpeg", size:'50×50' %> </td>
	    <td><%= link_to post.user.name, user_user_path(post.user.id) %></td>
	    <td><%= link_to post.body, user_post_path(post.id) %></td>
	      <% post.post_images.each do |post_image| %>
	        <td><%= attachment_image_tag post_image, :image, :fill, 50, 50, format:'jpeg' , fallback: "no_image.jpeg", size:'50×50' %> </td>
	      <% end %>
	    <td><%= link_to "#{post.comments.count} コメント", user_post_path(post.id) %><%= link_to "#{post.favorites.count}いいね！", user_post_path(post.id) %></td>
	  </tr>
	<% end %>
<% else %>
    <p>現在いいねしている投稿はありません。</p>
<% end %>