<h2>通報されたユーザー一覧</h2>
<% if @reports.present? %>
<!--% user = User.where(user_id:userreport.reported_user_id) %-->
	<% @reports.each do |report| %>

	<hr>
	<h4>プロフィール</h4>
	<p>プロフィール画像</p><p>アカウント名</p>
	<%= link_to (user_user_path(report.reported_user.id)) do %>
	  <%= attachment_image_tag report.reported_user, :image, format: 'jpeg',fallback: "no_image.jpg", size: "100x100" %>
	  <%= report.reported_user.name %>
    <% end %>
	<p>メールアドレス</p><%= report.reported_user.email %>
	<p>都道府県</p><%= report.reported_user.prefectures %>
	<p>通報内容</p>
	<%= report.report_message %>
	<% end %>
<% else %>
  <p>現在、通報されているユーザーはいません。</p>
<% end %>

